<%= form_for @comment, remote: true do |f| %>
    <%= f.hidden_field :question_id, :value => @question_id %>
    <%= f.text_field :content, placeholder: "Add a comment..." %>
    <%= f.submit "comment", class: "btn btn-success comment_submit" %>
<% end %>