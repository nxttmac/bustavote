<%= form_for(@question) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Ask new question...", style: "width:100%" %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>